# OSE Library Site

Static site generator for OSE part libraries (Schema Canon structure):
entry pages with parameters, 3D views, fabrication drawings, BOMs, and
validation status, regenerated from the libraries by CI. See docs/plan.md.

First deployment: https://opensourceecology.github.io/ose-library-site/
(vcs-library).
